<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activities.SeatBookActivity">

    <!-- Define a GridLayout to represent seats -->
    <GridLayout
        android:id="@+id/gridLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:columnCount="5"
        android:rowCount="5"
        android:padding="16dp"
        android:layout_margin="16dp"
        android:background="@drawable/border">

        <!-- Generate 25 buttons for 5x5 grid -->
        <Button
            android:id="@+id/seat1"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_columnWeight="1"
            android:layout_rowWeight="1"
            android:background="@drawable/seat_selector"
            android:text="1"/>

        <Button
            android:id="@+id/seat2"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_columnWeight="1"
            android:layout_rowWeight="1"
            android:background="@drawable/seat_selector"
            android:text="2"/>

        <!-- Repeat similar buttons for remaining seats (3 to 25) -->
        <!-- Make sure to give unique ids for each button -->

    </GridLayout>

    <!-- Button to confirm selection -->
    <Button
        android:id="@+id/confirmButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/gridLayout"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Confirm Selection" />

</RelativeLayout>
